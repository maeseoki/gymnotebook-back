pre-commit:
  commands:
    biome-check:
      glob: '*.{ts,tsx,js,jsx,json}'
      run: pnpm biome check --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    build:
      run: pnpm build
    test:
      run: pnpm test
